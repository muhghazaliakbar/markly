@AGENTS.md

## Claude Code notes

- Commit/PR attribution is turned off in `.claude/settings.json`; never add attribution lines by hand either.
- Project skills in `.claude/skills/`:
  - `run-and-verify` — build, launch and check the app without disturbing the developer.
  - `app-icon` — change and regenerate the app icon.
  - `release` — version bump, universal build, signing and notarisation checklist.
- Reply to the maintainer in the language they write in (usually Indonesian); keep code, commits and docs in English.
